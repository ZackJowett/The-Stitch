{
	"name": "the-stitch-testing-site",
	"version": "1.0.0",
	"description": "TESTING A website for The Stitch",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "npm i && cd functions && npm i && cd .. && sass public/styles.scss public/styles.css"
	},
	"author": "Zack Jowett",
	"license": "ISC",
	"dependencies": {
		"firebase": "^9.15.0"
	}
}
